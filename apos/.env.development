# Configurações específicas para ambiente de desenvolvimento

# Desabilitar a compressão do webpack para evitar erros de cache
NEXT_WEBPACK_DISABLE_COMPRESSION=1

# Usar polling para melhor compatibilidade com WSL
CHOKIDAR_USEPOLLING=1
WATCHPACK_POLLING=1

# Aumentar a memória disponível para Node
NODE_OPTIONS=--max-old-space-size=4096